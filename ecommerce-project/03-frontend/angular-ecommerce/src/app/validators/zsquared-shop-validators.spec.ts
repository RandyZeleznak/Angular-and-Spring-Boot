import { ZsquaredShopValidators } from './zsquared-shop-validators';

describe('ZsquaredShopValidators', () => {
  it('should create an instance', () => {
    expect(new ZsquaredShopValidators()).toBeTruthy();
  });
});
